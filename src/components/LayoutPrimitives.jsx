import styled from "styled-components"

const Body = styled.div`
  max-width: 1250px;
  margin: auto;
  font-family: Helvetica;
`

const Nav = styled.div`
  width: 100%;
  display: flex;
  align-items: center;
`

const NavLinks = styled.a`
  margin-right: 20px;
`

export { Body, Nav, NavLinks }
